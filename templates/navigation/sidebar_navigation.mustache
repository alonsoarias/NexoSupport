{{!
    Sidebar Navigation Template

    Renders the sidebar navigation tree with categories, items, and separators.

    Context variables required:
    * nodes - array of navigation nodes
    * has_nodes - boolean
    * active_node - currently active node object
    * breadcrumbs - array of breadcrumb nodes
    * has_breadcrumbs - boolean
    * depth - maximum tree depth

    @package core
    @template navigation/sidebar_navigation
}}
{{#has_nodes}}
<nav class="nexo-sidebar-nav" role="navigation" aria-label="{{#str}}sidebarnavigation, core{{/str}}">
    <ul class="nexo-sidebar-list">
        {{#nodes}}
        {{> navigation/sidebar_node}}
        {{/nodes}}
    </ul>
</nav>
{{/has_nodes}}
