{{!
    Upgrade Template

    Context:
    * dbversion - Database version
    * codeversion - Code version
    * release - Release version
    * upgradeexecuted - Boolean if upgrade was executed
    * upgradesuccess - Boolean if upgrade succeeded
    * upgradeerrors - Array of errors
    * haserrors - Boolean for errors
    * upgradeneeded - Boolean if upgrade is needed
    * sesskey - Session key
}}
<!DOCTYPE html>
<html lang="{{currentlang}}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#str}}upgrade,core{{/str}} - {{#str}}sitename,core{{/str}}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            margin: 0;
        }

        .container {
            background: white;
            border-radius: 10px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
            max-width: 700px;
            width: 100%;
            padding: 40px;
        }

        h1 {
            color: #333;
            margin-bottom: 10px;
            font-size: 28px;
        }

        h2 {
            color: #667eea;
            margin-bottom: 20px;
            font-size: 20px;
            font-weight: normal;
        }

        .version-info {
            background: #f5f5f5;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
        }

        .version-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid #e0e0e0;
        }

        .version-row:last-child {
            border-bottom: none;
        }

        .version-label {
            font-weight: 600;
            color: #555;
        }

        .version-value {
            color: #667eea;
            font-family: monospace;
            font-weight: bold;
        }

        .alert {
            padding: 15px;
            border-radius: 6px;
            margin: 20px 0;
        }

        .alert-info {
            background: #e3f2fd;
            color: #0277bd;
            border-left: 4px solid #0277bd;
        }

        .alert-success {
            background: #e8f5e9;
            color: #2e7d32;
            border-left: 4px solid #2e7d32;
        }

        .alert-error {
            background: #ffebee;
            color: #c62828;
            border-left: 4px solid #c62828;
        }

        .alert-warning {
            background: #fff3e0;
            color: #e65100;
            border-left: 4px solid #e65100;
        }

        .btn {
            display: inline-block;
            padding: 12px 30px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-decoration: none;
            border-radius: 6px;
            border: none;
            cursor: pointer;
            font-size: 16px;
            transition: transform 0.2s;
        }

        .btn:hover {
            transform: translateY(-2px);
        }

        .btn-secondary {
            background: #6c757d;
        }

        .actions {
            margin-top: 30px;
            display: flex;
            gap: 10px;
            justify-content: flex-end;
        }

        .upgrade-list {
            background: #f9f9f9;
            padding: 20px;
            border-radius: 6px;
            margin: 20px 0;
        }

        .upgrade-list ul {
            margin: 10px 0 10px 20px;
            line-height: 1.8;
        }

        .upgrade-list li {
            margin: 5px 0;
        }

        .error-list {
            margin: 10px 0;
            padding-left: 20px;
        }

        .checkmark {
            color: #2e7d32;
            font-size: 24px;
            margin-right: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{#str}}upgrade,core{{/str}}</h1>
        <h2>{{#str}}sitename,core{{/str}}</h2>

        <div class="version-info">
            <div class="version-row">
                <span class="version-label">{{#str}}currentversion,core{{/str}}:</span>
                <span class="version-value">{{{dbversion}}}</span>
            </div>
            <div class="version-row">
                <span class="version-label">{{#str}}newversion,core{{/str}}:</span>
                <span class="version-value">{{{codeversion}}}</span>
            </div>
            <div class="version-row">
                <span class="version-label">Release:</span>
                <span class="version-value">{{{release}}}</span>
            </div>
        </div>

        {{#upgradeexecuted}}
            {{#upgradesuccess}}
                <div class="alert alert-success">
                    <strong><span class="checkmark">✓</span>{{#str}}upgradecomplete,core{{/str}}!</strong><br>
                    {{#str}}systemreadyupgrade,core{{/str}}
                </div>

                <div class="actions">
                    <a href="/admin" class="btn">{{#str}}administration,core{{/str}}</a>
                </div>
            {{/upgradesuccess}}

            {{^upgradesuccess}}
                <div class="alert alert-error">
                    <strong>{{#str}}error,core{{/str}}</strong><br>
                    {{#str}}error,core{{/str}}:
                    <ul class="error-list">
                        {{#upgradeerrors}}
                            <li>{{{.}}}</li>
                        {{/upgradeerrors}}
                    </ul>
                </div>

                <div class="actions">
                    <a href="/admin/upgrade.php" class="btn">{{#str}}upgrade,core{{/str}}</a>
                    <a href="/admin" class="btn btn-secondary">{{#str}}back,core{{/str}}</a>
                </div>
            {{/upgradesuccess}}
        {{/upgradeexecuted}}

        {{^upgradeexecuted}}
            {{#upgradeneeded}}
                <div class="alert alert-warning">
                    <strong>{{#str}}requiresupgrade,core{{/str}}</strong><br>
                    {{#str}}systemreadyupgrade,core{{/str}}
                </div>

                <div class="upgrade-list">
                    <h3 style="margin-top: 0; color: #333;">Cambios en esta Actualización:</h3>
                    <h4 style="color: #667eea;">Versión 1.1.0 - Sistema RBAC Completo</h4>
                    <ul>
                        <li>✅ Sistema completo de Roles y Permisos (RBAC)</li>
                        <li>✅ Contextos jerárquicos (System, User, Course, Module)</li>
                        <li>✅ 3 Roles predefinidos (Administrator, Manager, User)</li>
                        <li>✅ 13 Capabilities del sistema</li>
                        <li>✅ Verificación granular de permisos</li>
                        <li>✅ Interfaz de gestión de roles</li>
                        <li>✅ Asignación automática de rol Administrator al usuario inicial</li>
                    </ul>
                </div>

                <div class="alert alert-info">
                    <strong>Importante:</strong><br>
                    • Se recomienda hacer un backup de la base de datos antes de actualizar<br>
                    • La actualización puede tardar unos segundos<br>
                    • No cierre esta ventana durante el proceso
                </div>

                <form method="POST">
                    <input type="hidden" name="upgrade" value="true">
                    <input type="hidden" name="sesskey" value="{{sesskey}}">

                    <div class="actions">
                        <a href="/admin" class="btn btn-secondary">{{#str}}cancel,core{{/str}}</a>
                        <button type="submit" class="btn">{{#str}}upgradenow,core{{/str}}</button>
                    </div>
                </form>
            {{/upgradeneeded}}

            {{^upgradeneeded}}
                <div class="alert alert-success">
                    <strong><span class="checkmark">✓</span>{{#str}}upgradecomplete,core{{/str}}</strong><br>
                    {{#str}}noupdaterequired,core{{/str}}
                </div>

                <div class="actions">
                    <a href="/admin" class="btn">{{#str}}administration,core{{/str}}</a>
                </div>
            {{/upgradeneeded}}
        {{/upgradeexecuted}}
    </div>
</body>
</html>
