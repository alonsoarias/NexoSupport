<div class="settings-section">
    <h3><i class="bi bi-plugin"></i> Plugins del Sistema</h3>

    <div class="alert alert-info">
        <i class="bi bi-info-circle"></i> Los plugins permiten extender la funcionalidad del sistema ISER.
        Los plugins marcados como <span class="badge bg-secondary">Core</span> no pueden ser deshabilitados.
    </div>

    <div class="table-responsive">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Plugin</th>
                    <th>Versión</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                {{#plugins}}
                <tr>
                    <td>
                        <strong>{{name}}</strong><br>
                        <small class="text-muted">{{plugin}}</small>
                        {{#is_core}}
                        <span class="badge bg-secondary">Core</span>
                        {{/is_core}}
                    </td>
                    <td>
                        <span class="badge bg-light text-dark">v{{version}}</span>
                    </td>
                    <td>
                        {{#enabled}}
                        <span class="badge bg-success">
                            <i class="bi bi-check-circle"></i> Habilitado
                        </span>
                        {{/enabled}}
                        {{^enabled}}
                        <span class="badge bg-danger">
                            <i class="bi bi-x-circle"></i> Deshabilitado
                        </span>
                        {{/enabled}}
                    </td>
                    <td>
                        {{^is_core}}
                        {{#enabled}}
                        <form method="POST" action="/admin/plugins.php" style="display: inline;">
                            <input type="hidden" name="action" value="disable">
                            <input type="hidden" name="plugin" value="{{plugin}}">
                            <button type="submit" class="btn btn-sm btn-warning">
                                <i class="bi bi-pause-circle"></i> Deshabilitar
                            </button>
                        </form>
                        {{/enabled}}
                        {{^enabled}}
                        <form method="POST" action="/admin/plugins.php" style="display: inline;">
                            <input type="hidden" name="action" value="enable">
                            <input type="hidden" name="plugin" value="{{plugin}}">
                            <button type="submit" class="btn btn-sm btn-success">
                                <i class="bi bi-play-circle"></i> Habilitar
                            </button>
                        </form>
                        {{/enabled}}
                        {{/is_core}}
                        {{#is_core}}
                        <span class="text-muted">—</span>
                        {{/is_core}}
                    </td>
                </tr>
                {{/plugins}}
                {{^plugins}}
                <tr>
                    <td colspan="4" class="text-center text-muted py-4">
                        <i class="bi bi-inbox"></i><br>
                        No hay plugins registrados
                    </td>
                </tr>
                {{/plugins}}
            </tbody>
        </table>
    </div>
</div>

<div class="settings-section mt-4">
    <h4><i class="bi bi-cloud-download"></i> Instalar Nuevo Plugin</h4>
    <p class="text-muted">Puedes instalar nuevos plugins desde un archivo ZIP.</p>
    <a href="/admin/tool/installaddon/index.php" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> Instalar Plugin
    </a>
</div>
