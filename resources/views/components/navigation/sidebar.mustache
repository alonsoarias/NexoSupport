<aside class="sidebar" id="sidebar">
    <nav class="sidebar-nav">
        <!-- Inicio -->
        <div class="nav-section">
            <ul class="nav-items">
                <li class="nav-item">
                    <a href="/" class="nav-link {{#navigation.is_home}}active{{/navigation.is_home}}">
                        <i class="bi bi-house-door"></i>
                        <span>Inicio</span>
                    </a>
                </li>
            </ul>
        </div>

        <!-- Administración -->
        <div class="nav-section">
            <div class="nav-section-title">Administración</div>
            <ul class="nav-items">
                <li class="nav-item">
                    <a href="/admin" class="nav-link {{#navigation.is_admin_dashboard}}active{{/navigation.is_admin_dashboard}}">
                        <i class="bi bi-speedometer2"></i>
                        <span>Dashboard Admin</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/users" class="nav-link {{#navigation.is_users}}active{{/navigation.is_users}}">
                        <i class="bi bi-people"></i>
                        <span>Usuarios</span>
                        {{#navigation.users_count}}
                        <span class="nav-badge">{{navigation.users_count}}</span>
                        {{/navigation.users_count}}
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/roles" class="nav-link {{#navigation.is_roles}}active{{/navigation.is_roles}}">
                        <i class="bi bi-shield-check"></i>
                        <span>Roles</span>
                        {{#navigation.roles_count}}
                        <span class="nav-badge">{{navigation.roles_count}}</span>
                        {{/navigation.roles_count}}
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/permissions" class="nav-link {{#navigation.is_permissions}}active{{/navigation.is_permissions}}">
                        <i class="bi bi-key"></i>
                        <span>Permisos</span>
                        {{#navigation.permissions_count}}
                        <span class="nav-badge">{{navigation.permissions_count}}</span>
                        {{/navigation.permissions_count}}
                    </a>
                </li>
            </ul>
        </div>

        <!-- Sistema -->
        <div class="nav-section">
            <div class="nav-section-title">Sistema</div>
            <ul class="nav-items">
                <li class="nav-item">
                    <a href="/admin/settings" class="nav-link {{#navigation.is_settings}}active{{/navigation.is_settings}}">
                        <i class="bi bi-gear"></i>
                        <span>Configuración</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/reports" class="nav-link {{#navigation.is_reports}}active{{/navigation.is_reports}}">
                        <i class="bi bi-bar-chart"></i>
                        <span>Reportes</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/security" class="nav-link {{#navigation.is_security}}active{{/navigation.is_security}}">
                        <i class="bi bi-shield-lock"></i>
                        <span>Seguridad</span>
                    </a>
                </li>
            </ul>
        </div>

        <!-- Usuario -->
        <div class="nav-section nav-section-bottom">
            <ul class="nav-items">
                <li class="nav-item">
                    <a href="/profile" class="nav-link {{#navigation.is_profile}}active{{/navigation.is_profile}}">
                        <i class="bi bi-person-circle"></i>
                        <span>Mi Perfil</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/logout" class="nav-link">
                        <i class="bi bi-box-arrow-right"></i>
                        <span>Cerrar Sesión</span>
                    </a>
                </li>
            </ul>
        </div>
    </nav>
</aside>

<!-- Sidebar Overlay (Mobile) -->
<div class="sidebar-overlay" id="sidebarOverlay"></div>
