<!-- Configuración del Sistema -->
<div class="admin-container">
    <div class="admin-header">
        <div>
            <h2>Configuración del Sistema</h2>
            <p class="text-muted">Configuración general y parámetros del sistema</p>
        </div>
        <div class="user-actions">
            <a href="/admin" class="btn btn-sm btn-secondary">
                <i class="bi bi-arrow-left"></i> Volver
            </a>
        </div>
    </div>

    <!-- Información del Sistema (DATOS REALES) -->
    <div class="settings-section">
        <h3><i class="bi bi-info-circle"></i> Información del Sistema</h3>
        <div class="settings-grid">
            <div class="setting-item">
                <div class="setting-label">Nombre de la Aplicación</div>
                <div class="setting-value">{{config.app_name}}</div>
            </div>

            <div class="setting-item">
                <div class="setting-label">Versión</div>
                <div class="setting-value">{{config.app_version}}</div>
            </div>

            <div class="setting-item">
                <div class="setting-label">Versión de PHP</div>
                <div class="setting-value">{{config.php_version}}</div>
            </div>

            <div class="setting-item">
                <div class="setting-label">Driver de Base de Datos</div>
                <div class="setting-value">{{config.db_driver}}</div>
            </div>

            <div class="setting-item">
                <div class="setting-label">Zona Horaria</div>
                <div class="setting-value">{{config.timezone}}</div>
            </div>
        </div>
    </div>

    <!-- Configuración de Seguridad -->
    <div class="settings-section">
        <h3><i class="bi bi-shield-lock"></i> Configuración de Seguridad</h3>
        <div class="info-box info-warning">
            <i class="bi bi-exclamation-triangle"></i>
            <div>
                <strong>Importante:</strong> La configuración de seguridad se gestiona a través del archivo <code>.env</code>
                ubicado en el directorio raíz del proyecto. Edite este archivo con precaución.
            </div>
        </div>

        <div class="security-items">
            <div class="security-item">
                <div class="security-icon">
                    <i class="bi bi-key-fill"></i>
                </div>
                <div class="security-content">
                    <h4>Contraseñas</h4>
                    <ul>
                        <li>Algoritmo de hash: <strong>bcrypt</strong></li>
                        <li>Longitud mínima: <strong>8 caracteres</strong></li>
                        <li>Requiere mayúsculas, minúsculas, números y caracteres especiales</li>
                    </ul>
                </div>
            </div>

            <div class="security-item">
                <div class="security-icon">
                    <i class="bi bi-shield-check"></i>
                </div>
                <div class="security-content">
                    <h4>Autenticación</h4>
                    <ul>
                        <li>Bloqueo de cuenta: <strong>5 intentos fallidos</strong></li>
                        <li>Duración del bloqueo: <strong>15 minutos</strong></li>
                        <li>Registro de intentos de login activo</li>
                    </ul>
                </div>
            </div>

            <div class="security-item">
                <div class="security-icon">
                    <i class="bi bi-clock-history"></i>
                </div>
                <div class="security-content">
                    <h4>Sesiones</h4>
                    <ul>
                        <li>Duración: <strong>2 horas</strong></li>
                        <li>HttpOnly: <strong>Activado</strong></li>
                        <li>SameSite: <strong>Lax</strong></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <!-- Archivos de Configuración -->
    <div class="settings-section">
        <h3><i class="bi bi-file-earmark-text"></i> Archivos de Configuración</h3>
        <div class="info-box info-info">
            <i class="bi bi-info-circle"></i>
            <div>
                <p><strong>Ubicación de archivos importantes:</strong></p>
                <ul>
                    <li><code>.env</code> - Variables de entorno y configuración principal</li>
                    <li><code>database/schema/schema.xml</code> - Esquema de base de datos</li>
                    <li><code>config/app.php</code> - Configuración de la aplicación</li>
                </ul>
            </div>
        </div>
    </div>
</div>

<style>
.admin-container {
    padding: 20px 0;
    max-width: 1400px;
    margin: 0 auto;
}

.admin-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 30px;
    padding-bottom: 20px;
    border-bottom: 2px solid var(--border-color);
}

.admin-header h2 {
    color: var(--text-primary);
    margin: 0 0 5px 0;
}

.text-muted {
    color: var(--text-secondary);
    margin: 0;
}

.user-actions {
    display: flex;
    gap: 10px;
}

.btn-sm {
    padding: 8px 16px;
    font-size: 0.875rem;
}

.btn-secondary {
    background: var(--bg-secondary);
    color: var(--text-primary);
}

.btn-secondary:hover {
    background: var(--border-color);
}

/* Settings Sections */
.settings-section {
    background: var(--bg-secondary);
    border-radius: 12px;
    padding: 30px;
    margin-bottom: 30px;
}

.settings-section h3 {
    color: var(--text-primary);
    margin: 0 0 25px 0;
    display: flex;
    align-items: center;
    gap: 10px;
}

.settings-section h3 i {
    color: var(--iser-green);
}

/* Settings Grid */
.settings-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 20px;
}

.setting-item {
    padding: 15px;
    background: var(--bg-primary);
    border-radius: 8px;
    border-left: 3px solid var(--iser-green);
}

.setting-label {
    font-size: 0.875rem;
    color: var(--text-secondary);
    margin-bottom: 5px;
}

.setting-value {
    font-size: 1.1rem;
    color: var(--text-primary);
    font-weight: 600;
}

/* Info Boxes */
.info-box {
    padding: 20px;
    border-radius: 8px;
    display: flex;
    gap: 15px;
    margin-bottom: 25px;
}

.info-box i {
    font-size: 1.5rem;
    flex-shrink: 0;
}

.info-box ul {
    margin: 10px 0 0 20px;
    padding: 0;
}

.info-box li {
    margin: 5px 0;
}

.info-box.info-warning {
    background: #fff3cd;
    color: #856404;
    border-left: 4px solid #ffc107;
}

.info-box.info-info {
    background: #d1ecf1;
    color: #0c5460;
    border-left: 4px solid #17a2b8;
}

/* Security Items */
.security-items {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 20px;
}

.security-item {
    padding: 20px;
    background: var(--bg-primary);
    border-radius: 8px;
    display: flex;
    gap: 15px;
}

.security-icon {
    font-size: 2.5rem;
    color: var(--iser-green);
    flex-shrink: 0;
}

.security-content h4 {
    color: var(--text-primary);
    margin: 0 0 10px 0;
}

.security-content ul {
    margin: 0;
    padding: 0 0 0 20px;
}

.security-content li {
    color: var(--text-secondary);
    margin: 8px 0;
}

.security-content strong {
    color: var(--text-primary);
}

code {
    background: var(--bg-primary);
    padding: 2px 6px;
    border-radius: 4px;
    font-family: 'Courier New', monospace;
    font-size: 0.9em;
    color: var(--iser-green);
}
</style>
